#include <unistd.h>
#include <conio.h>

int	ft_strlen(char *str)
{
int b;

	b = 0;
	while(str[b] != '\0')
	{
	b++;
	}
	return(b);
}
int main(){
	
int	ft_strlen(ahmed);
	getch();
	return(0);
}
